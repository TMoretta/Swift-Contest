----region ROW LEVEL SECURITY FOR MESSAGES
---- Enable Row Level Security on the messages table.
--ALTER TABLE public.messages ENABLE ROW LEVEL SECURITY;
--
---- Drop existing policies to ensure a clean state.
--DROP POLICY IF EXISTS "Allow owner to read their own messages" ON public.messages;
--DROP POLICY IF EXISTS "Allow owner to update their own messages" ON public.messages;
--DROP POLICY IF EXISTS "Allow owner to delete their own messages" ON public.messages;
---- Also drop the old permissive policy if it exists
--DROP POLICY IF EXISTS "Messages: allow all" ON public.messages;
--
--
----region SELECT POLICY
---- Allow a user to read ONLY their own messages.
--CREATE POLICY "Allow owner to read their own messages"
--ON public.messages
--FOR SELECT
--TO authenticated
--USING (account_id = auth.uid());
----endregion
--
----region UPDATE POLICY
---- Allow a user to update ONLY their own messages.
--CREATE POLICY "Allow owner to update their own messages"
--ON public.messages
--FOR UPDATE
--TO authenticated
--USING (account_id = auth.uid())
--WITH CHECK (account_id = auth.uid());
----endregion
--
----region DELETE POLICY
---- Allow a user to delete ONLY their own messages.
--CREATE POLICY "Allow owner to delete their own messages"
--ON public.messages
--FOR DELETE
--TO authenticated
--USING (account_id = auth.uid());
----endregion
--
----region COLUMN-LEVEL PERMISSIONS FOR UPDATE
---- Grant permission to update only the 'is_read' status.
---- This prevents users from changing the title or body of a system-generated message.
--REVOKE UPDATE ON public.messages FROM authenticated;
--GRANT UPDATE (is_read) ON public.messages TO authenticated;
----endregion
--
---- NOTE: No INSERT policy is defined for users.
---- Message creation is handled by trusted server-side logic (e.g., SECURITY DEFINER functions)
---- to ensure messages are generated correctly and sent to the appropriate recipients.
----endregion